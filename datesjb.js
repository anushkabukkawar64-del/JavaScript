// let numbers = [1, 2, 3, 4, 5];
// console.log(numbers); // prints the entire array

// console.log(numbers[0]) // prints the first element of the array
// console.log(numbers[numbers.length - 1]) // print last element of the array
// console.log(numbers[numbers.length - 1 / 2]) // middle 

// let numbers = [1, 2, 3, 4, 5 , 6];  
// console.log(numbers);
// // number.length > 5 - 1 
// console.log(numbers[0]); //print 1st element of the array
// console.log(numbers[numbers.length - 1]); //print last element of the array
// console.log(numbers.length - 1) / 2; //print middle element of the array
// console.log(numbers[numbers.length / 2]); 

let numbers_even = [1, 2, 3, 4, 5, 6];
let numbers_odd = [ 1, 2, 3, 4, 5];
console.log(numbers_even); // print entire array
console.log(numbers_even);
// numbers.length > 5 -1
console.log(numbers_even[0]);   // print first element of the array
console.log(numbers_even[numbers_even.length - 1]); // print last element of 

console.log("odd: ", numbers_odd[(numbers_odd.length - 1) / 2]); // middle--> if odd
console.log(" even: ", numbers_even[(numbers_even.length) / 2]); // middle --> if even

